FILE_LOCATION="/var/lib/abcd"
LIBEXEC="/usr/lib/abcd"
NETBOOT_CLIENTS="/etc/abcd/clients"
BASEURL="http://$(host -t SRV _netboot._autoprovision | rev | cut -f1 -d" " | cut -b2- | rev)"
POSTINSTALL="${FILE_LOCATION}/patches/postinstall"
PUPPETSERVER="$(host -t SRV _puppet._https | rev | cut -f1 -d" " | cut -b2- | rev)"
ROOTPASSWORD="changeme"
DEFAULT_DEPLOYMENT="debian/squeeze"
DEFAULT_DEPLOYMENT_ENABLED="true"
